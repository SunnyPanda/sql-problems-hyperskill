-- MySQL has the key_cache_block_size system variable that shows the size of blocks in the key cache in bytes.
-- We want to know its value. Write a command that will show it.

SELECT @@key_cache_block_size